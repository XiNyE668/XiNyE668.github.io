name: FontHub V4 Build

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'fonthub-v3/**'
      - '.github/workflows/fonthub-v3-build.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Prepare upstream source
        run: |
          set -euo pipefail
          git clone --depth 1 https://github.com/Numbersf/MakeFontsGreatAgain.git upstream
          cat fonthub-v3/parts/patch.* | base64 -d > /tmp/fonthub-v4-patch.zip
          echo 'bb109f01f31acbf865713bee2e3946bde4c5251e696cf6d6ec5a2a3aa3a5ec33  /tmp/fonthub-v4-patch.zip' | sha256sum -c -
          unzip -t /tmp/fonthub-v4-patch.zip
          unzip -qo /tmp/fonthub-v4-patch.zip -d /tmp/fonthub-v4-patch
          cp -a /tmp/fonthub-v4-patch/app/. upstream/mfga-xposed/app/

          grep -RIlZ 'com\.mfga\.xposed' upstream/mfga-xposed/app | \
            xargs -0 -r sed -i 's/com\.mfga\.xposed/billionaire.xinye.xposed/g'

          mkdir -p upstream/mfga-xposed/app/src/main/res/drawable-nodpi
          base64 -d fonthub-v3/icon.b64 > upstream/mfga-xposed/app/src/main/res/drawable-nodpi/fonthub_icon.png

          ! grep -RIl 'com\.mfga\.xposed' upstream/mfga-xposed/app
          grep -n 'applicationId = "billionaire.xinye.xposed"' upstream/mfga-xposed/app/build.gradle.kts
          grep -n 'versionCode = 40' upstream/mfga-xposed/app/build.gradle.kts
          grep -n 'versionName = "4.0"' upstream/mfga-xposed/app/build.gradle.kts

      - name: Build release APK
        working-directory: upstream/mfga-xposed
        run: gradle :app:assembleRelease --stacktrace --no-daemon

      - name: Align and sign APK
        run: |
          set -euo pipefail
          UNSIGNED=$(find upstream/mfga-xposed/app/build/outputs/apk/release -name '*.apk' | head -1)
          test -n "$UNSIGNED"
          BT=$(ls "$ANDROID_HOME/build-tools" | sort -V | tail -1)
          "$ANDROID_HOME/build-tools/$BT/zipalign" -f -p 4 "$UNSIGNED" FontHub-V4-aligned.apk
          curl -fsSL https://raw.githubusercontent.com/mfonville/pa_gapps/master/testkey.pk8 -o testkey.pk8
          curl -fsSL https://raw.githubusercontent.com/mfonville/pa_gapps/master/testkey.x509.pem -o testkey.x509.pem
          "$ANDROID_HOME/build-tools/$BT/apksigner" sign \
            --key testkey.pk8 --cert testkey.x509.pem \
            --v1-signing-enabled true --v2-signing-enabled true --v3-signing-enabled true \
            --out FontHub-XPosed-V4-A16.apk FontHub-V4-aligned.apk
          "$ANDROID_HOME/build-tools/$BT/apksigner" verify --verbose --print-certs FontHub-XPosed-V4-A16.apk
          "$ANDROID_HOME/build-tools/$BT/aapt" dump badging FontHub-XPosed-V4-A16.apk | head -8 || true
          "$ANDROID_HOME/build-tools/$BT/aapt" dump badging FontHub-XPosed-V4-A16.apk | grep "package: name='billionaire.xinye.xposed' versionCode='40' versionName='4.0'"
          sha256sum FontHub-XPosed-V4-A16.apk > FontHub-XPosed-V4-A16.apk.sha256

      - uses: actions/upload-artifact@v4
        with:
          name: FontHub-XPosed-V4-A16
          path: |
            FontHub-XPosed-V4-A16.apk
            FontHub-XPosed-V4-A16.apk.sha256
          if-no-files-found: error
